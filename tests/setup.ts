// Register @testing-library/jest-dom custom matchers (e.g. toBeInTheDocument,
// toHaveTextContent) on Vitest's `expect`. The `/vitest` entry point is the
// supported integration in @testing-library/jest-dom v6+.
import "@testing-library/jest-dom/vitest";
